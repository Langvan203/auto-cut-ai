# Auto Cut AI - Video Merging Tool
#
# Không cần cài thêm package Python để chạy ứng dụng (sử dụng thư viện built-in)
# Yêu cầu: FFmpeg phải được cài đặt và có trong system PATH
#
# Cài FFmpeg:
# - Windows: https://www.gyan.dev/ffmpeg/builds/ (tải bản full)
# - macOS: brew install ffmpeg
# - Linux: sudo apt install ffmpeg
#
# Để đóng gói thành file .exe, cần cài thêm:
pyinstaller>=6.0
